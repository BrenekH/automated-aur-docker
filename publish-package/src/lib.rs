pub fn publish() {
    // Read manifest

    // Clone existing AUR repo into temp dir

    // Configure Git (user.name and user.email)

    // Copy PKGBUILD and manifest.include files to cloned repo

    // Create .SRCINFO file in cloned repo

    // Overwrite .gitignore

    // Force add PKGBUILD, .SRCINFO, .gitignore, and mani.include files.

    // Commit files to cloned repo

    // Push new commit to AUR
}
